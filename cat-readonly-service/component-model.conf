subsystem = DMS
component = CatReadOnlyService
modelVersion = "3.0"

wbsId = TMT.DMS.CAT

title = "Read-Only Catalog Service"
componentType = Service

description = """
      The Read-Only Catalog Service is ???.
      The objects in the Read-Only Catalog are accssed primarily through a) an IVOA Cone Search API that allows
      searching an area around a position specified in the sky with a specific radius and b) an IVOA TAP (Table Access
      Protocol) service API.
"""